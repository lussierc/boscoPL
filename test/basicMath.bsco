?? Bosco Language - Sample Program 2
?? Contains simple math operations
woof basicMath:
  bone doMath -int a, double b, bool c- (
    ?? Perform addition:
    int x = a + b|
    int y = x + c|

    ?? Perform subtraction:
    int z = y - a|

    ?? Perform multiplication:
    int mult = z * x|

    ?? Print the last result to terminal:
    speak-mult|
  ) ?? End of bone
:
?? End of program
